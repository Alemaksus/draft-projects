package lambda;

// The Interface is Functional because of in contents only one Abstract method (starting from
// Java8 can have a realization):

public interface CheckTrait {
  boolean test(Animal a);
}
